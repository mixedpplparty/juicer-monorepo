import styled from "@emotion/styled";

export const Main = styled.main({
	display: "flex",
	flexDirection: "column",
	gap: "12px",
	overflowY: "auto",
	maxHeight: "100%",
	position: "relative",
	zIndex: 1,
});
