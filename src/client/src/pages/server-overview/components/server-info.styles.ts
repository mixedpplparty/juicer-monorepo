import { css } from "@emotion/react";

export const serverInfoStyles = {
	root: css({
		display: "flex",
		flexDirection: "column",
		gap: "2rem",
	}),
	section: css({
		display: "flex",
		flexDirection: "column",
		gap: "0.75rem",
	}),
	heading: css({
		margin: 0,
	}),
};
