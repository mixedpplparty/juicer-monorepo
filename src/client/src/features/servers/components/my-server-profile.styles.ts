import { css } from "@emotion/react";

export const myServerProfileStyles = {
	root: css({
		display: "flex",
		width: "100%",
		flexDirection: "column",
	}),
	nicknameRow: css({
		display: "flex",
		width: "100%",
		flexDirection: "row",
		alignItems: "center",
		gap: "0.5rem",
	}),
};
