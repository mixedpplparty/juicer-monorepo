import { css } from "@emotion/react";

export const topicCategoryDialogStyles = {
	form: css({
		display: "flex",
		flexDirection: "column",
		gap: "1.25rem",
	}),
	field: css({
		width: "100%",
	}),
};
