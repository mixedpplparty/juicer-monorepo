# React + TypeScript + Vite

This project uses React, TypeScript, and Vite with HMR.

The React Compiler is not enabled because of its impact on development and build performance. See the [React Compiler documentation](https://react.dev/learn/react-compiler/installation) for setup guidance.
